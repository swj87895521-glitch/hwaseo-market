export type Store = {
  id: string;
  slug: string;
  name: string;
  category: string;
  shortDescription: string;
  addressText: string;
  locationText: string;
  thumbnail: string;
  markerId: string;
  featuredProductIds: string[];
  isActive: boolean;
};
